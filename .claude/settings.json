{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(*git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/elwin/code/handless && bun run lint 2>&1 && bun run format:check 2>&1 && bun run check:translations 2>&1"
          }
        ]
      }
    ]
  }
}
